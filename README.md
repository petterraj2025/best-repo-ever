# best-repo-ever
Step 1: Create a Branch
To see a list of local branches in your terminal, type git branch. Right now, you probably only see one branch, main. 

Let’s create a new branch (myfeaturebranch) for our work:

Create the branch:
git branch myfeaturebranch
Checkout to that branch:
git checkout myfeaturebranch
git checkout -b new-branch-1 TEST1 cPET